class circle:
    def __init__(self, radius):
        self.radius = radius
    
    def area(radius):
        return np.pi * (self.radius ** 2)
    
    def perimeter(self):
        return np.pi * 2 * self.radius